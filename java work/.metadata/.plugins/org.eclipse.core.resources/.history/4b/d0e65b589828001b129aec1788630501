package day02;

public class ArrayExam02 {
	public static void main(String[] args) {
		for(String s : args) {
			System.out.println(s);
		}
		
		Integer.parseInt(args[0]); // 형변환 and 객체가 아닌 것들을 객체로 만들어줌.
		
		PlusMachine pm = new PlusMachine();
		pm.evenSum(args[0], args[1]);
	}
}
